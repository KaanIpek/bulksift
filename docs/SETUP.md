# Connecting the services

Three of the four are **done** and their keys are in the code. What is left is
listed at the bottom, and each remaining item needs a secret that should not
pass through an agent's context.

| Service | State | Where the key lives |
|---|---|---|
| **AdMob** | Done | `ads.ts`, `app.json` |
| **Supabase** | Done | `auth.ts` |
| **App Store Connect products** | Done | ids match `store.ts` |
| **RevenueCat** | Blocked on an in-app purchase key | `store.ts` (still null) |
| **Price hosting** | Not started | `pricesStore.ts` (still null) |

---

## Done: AdMob

iOS app **BulkSift**, one rewarded unit granting 10 scans.

- App ID `ca-app-pub-8253427588583765~6401609975` - in `app.json`
- Rewarded unit `ca-app-pub-8253427588583765/6328673968` - in `ads.ts`

The Android app id was removed rather than left pointing at Google's test id:
BulkSift is iOS-only, and a test id in a shipped build is against AdMob's terms.

**Still to do by hand:** link a payment profile in AdMob, or the unit serves
nothing. New units also sit in "limited ad serving" until the app is reviewed,
which needs a store listing.

## Done: Supabase

Project **bulksift** - `https://jyauggpngoihtpqlnsme.supabase.co`

- The publishable key is in `auth.ts`. It is meant to ship in the app: it
  identifies the project and authorises nothing on its own.
- `collections` table created, row-level security on, policy `auth.uid() =
  user_id`, index on `user_id`. That policy is what actually protects the data.
- **Apple** and **Email** sign-in are both enabled. Apple needed only the bundle
  id in Client IDs, because the app signs in natively and hands Supabase an
  identity token - the OAuth secret is for web sign-in, which this app does not
  use.

The database password was generated by Supabase and never read. Nothing in the
app uses it; reset it from Settings > Database if you ever need it.

**Still to do by hand:** deploy the account-deletion function, which is in this
repo and required by App Review:

```bash
supabase functions deploy delete-account
```

## Done: App Store Connect products

All five, priced, localised, and available in all 175 countries.

| Product ID | Type | Price |
|---|---|---|
| `com.rldgames.bulksift.credits.200` | Consumable | $1.99 |
| `com.rldgames.bulksift.credits.1000` | Consumable | $5.99 |
| `com.rldgames.bulksift.credits.5000` | Consumable | $14.99 |
| `com.rldgames.bulksift.pro.monthly` | Subscription, 1 month | $4.99 |
| `com.rldgames.bulksift.pro.yearly` | Subscription, 1 year | $29.99 |

Both subscriptions are in the group **BulkSift Pro**.

The prices came from what the competition actually charges. The two direct
scanner apps - Card Value Scanner (20K ratings) and PokeScope (2.5K) - both sell
a **weekly** subscription at $7.99 and an annual at $24.99. A weekly at that
price annualises to about $415, and PokeScope's reviews complain about exactly
that. No weekly product was created here on purpose. Collectr, the reputable
portfolio app, is $7.99/month or $59.99/year, and gates unlimited scanning
behind it in the same way.

Nobody sells one-time scan credits. Someone with a single bulk box to get
through does not want a subscription, and that is the gap the packs fill:
5,000 scans for $14.99 is 0.3 cents a card.

**Still to do by hand:** the first consumable and the first subscription must be
submitted together with an app version, and selling anything at all requires the
**Paid Applications agreement** to be signed under Business.

---

## Left: RevenueCat

Project **BulkSift** exists with the App Store app form filled in, and it will
not save without an **In-App Purchase Key**. That key is a `.p8` file downloaded
once from App Store Connect and it is a credential, so it was left alone
deliberately.

1. App Store Connect > Users and Access > Integrations > **In-App Purchase**,
   generate a key, download the `.p8`. It can only be downloaded once.
2. RevenueCat's open form wants the Key ID, the Issuer ID and that file.
3. Then create an entitlement with the identifier **`pro`** - exactly that
   string, `PRO_ENTITLEMENT` in `store.ts` reads it - and attach both
   subscriptions.
4. Create an offering containing both subscriptions.
5. Copy the **public SDK key for Apple** (starts `appl_`) into:

```ts
// apps/mobile/src/store.ts
export const REVENUECAT_IOS_KEY: string | null = 'appl_xxxxxxxxxxxxxxxxxxxx';
```

Until then the app reports "Not connected" on the Purchases row rather than
offering a button that fails.

## Left: somewhere to host `prices.json`

**Why:** so "Refresh prices now" has something to fetch. Until then the app uses
the snapshot baked into the build, which is correct but frozen.

The file is 1.73 MB, and **0.26 MB gzipped**. At 10,000 users refreshing daily
that is about 79 GB a month.

**Cloudflare R2** is the recommendation - zero egress fees, so the whole thing
costs cents for storage and nothing for traffic. A plain bucket with public read
is enough; there is no API, just a file. The Cloudflare account is not signed in
on this machine.

```ts
// apps/mobile/src/pricesStore.ts
export const PRICE_HOST: string | null = 'https://prices.bulksift.app';
```

Two files go in the bucket, side by side:

- `prices.json` - the whole book.
- `prices-meta.json` - a few bytes carrying the same `updated` date.

The app fetches the manifest first and only pulls the book when that date has
moved, so a refresh that finds nothing new transfers almost nothing. Uploading
`prices.json` without updating the manifest means no device ever notices it -
which is why `npm run prices` now writes both.

## Left: a privacy policy page

Settings links to `https://bulksift.app/privacy`, which does not exist. A dead
link there is a rejection. A single static page is enough, and it needs a
contact route on it.

---

## What is deliberately not sold

Price freshness. It costs cents to serve, so gating it would be charging for
something that is free to give - and an app that shows stale prices to free users
is an app whose numbers cannot be trusted by anyone. Pro sells unlimited
scanning, unlimited collections and export.

---

## Checking

Each service reports itself in the app, so this needs no separate tooling:

- **Settings > Account** offers Continue with Apple instead of "Sync - Not
  connected". This should work now.
- **Settings > Purchases** reads "Restore purchases" instead of "Not connected".
  Waiting on RevenueCat.
- **Settings > Prices** shows a date, and Refresh moves it. Waiting on a host.
- Ads: the scan screen offers a video when the daily free scans run out.
